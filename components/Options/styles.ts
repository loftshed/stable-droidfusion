import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  sectionTitle: {
    fontSize: 24,
    fontWeight: '600',
    textAlign: 'center',
  },
  textInput: {
    padding: 30,
    backgroundColor: 'yellow',
  },
});
